Node.js server consuming https://restcountries.com/ api - return one country

## Install dependencies
run "npm install" in root directory

## Start application
run "node app.js"

## Open app
app is running at http://localhost:3000/
